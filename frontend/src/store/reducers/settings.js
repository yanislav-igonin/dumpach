const initialState = {
  threads: {
    limitPerPage: 10
  },
  app: {
    pageId: 1
  }
};

const settings = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default settings;
