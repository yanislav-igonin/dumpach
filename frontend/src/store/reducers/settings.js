const initialState = {
  pagination: {
    threadsPerPage: 10,
    pageId: 1
  },
};

const settings = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default settings;
