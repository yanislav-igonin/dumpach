// const db = require('../../db/connection/');

module.exports = {
  async login(req, res) {
    console.log(req);
    res.send('ok');
  },

  async logout(req, res) {
    console.log(req);
    res.send('ok');
  },
};
